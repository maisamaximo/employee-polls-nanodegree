import {connect} from "react-redux";
import './style.css';

const Login = (props) => {
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
}

const mapStateToProps = ({}) => ({});

export default connect(mapStateToProps)(Login);
